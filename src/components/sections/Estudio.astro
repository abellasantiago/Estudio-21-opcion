---
import '../../styles/estudio.css';
import TeamMember from '../TeamMember.astro';
import { equipo } from '../../data/equipo';
---

<section class="block alt" id="estudio">
  <span class="marks" aria-hidden="true">
    <span class="tl"></span><span class="tr"></span><span class="bl"></span><span class="br"></span>
  </span>
  <div class="wrap">
    <div class="sec-head">
      <span class="eyebrow reveal">Nuestro equipo</span>
      <h2 class="section-title reveal" data-d="1">Seis personas detrás de cada proyecto.</h2>
      <p class="lead reveal" data-d="2">
        Nuestro equipo está formado por profesionales de distintas áreas que
        comparten un mismo objetivo: transformar ideas en soluciones reales, con
        atención al detalle y foco en las necesidades de nuestros clientes.
      </p>
    </div>

    <div class="team-grid">
      {equipo.map((p, i) => <TeamMember persona={p} d={(i % 3) + 1} />)}
    </div>
  </div>
</section>
